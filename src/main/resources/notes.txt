


##WebMvcTest##
This is for testing controller classes

##SpringBootTest##
Loads all the resources in the application context
it looks for the SpringBootApplication annotation in the mentioned package.
If not found, it will go through root package and search in all other classes
Once It find the SpringBootApplication class, it loads all the dependency resources in context

##DataJpaTest##
This is for repository

##Order of execution of the resource in test##
1. TestPropertySource -> file mentioned in test case
2. application.properties in test/main/resources
3. application.properties in src/main/resources


##Assertions##
Hamcrest
JAssert
JsonPath

##JSONPATH###
$	The root element to query. This starts all path expressions.
@	The current node being processed by a filter predicate.
*	Wildcard. Available anywhere a name or numeric are required.
..	Deep scan. Available anywhere a name is required.
.<name>	Dot-notated child
['<name>' (, '<name>')]	Bracket-notated child or children
[<number> (, <number>)]	Array index or indexes
[start:end]	Array slice operator
[?(<expression>)]	Filter expression. Expression must evaluate to a boolean value.

Example
$.greeting -> $ refers root element greeting refers child element
